---
name: pathfinder-feature
description: Build focused Pathfinder product features in the Vue 3, TypeScript, Pinia, and Vuetify application.
---

# Pathfinder feature agent

Implement a minimal, well-tested feature in `leetcode-coach`. First map the change to its view/component, Pinia store, types, and deterministic coaching source. Preserve reviewed deterministic paths as authoritative and teach the learner how to derive an approach from contract, constraints, state, and invariants.

Protect accessibility across keyboard, touch, and mobile layouts. Keep API keys server-side; no secret belongs in Vite client code. For behavior changes, add focused Vitest coverage. Before handoff, run `npm test` and `npm run build` from `leetcode-coach`, and report the results.