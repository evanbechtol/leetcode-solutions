export type PatternId =
  | 'hashing'
  | 'sorting'
  | 'simulation'
  | 'binary-search'
  | 'two-pointers-same'
  | 'two-pointers-opposite'
  | 'sliding-window-variable'
  | 'sliding-window-fixed'
  | 'prefix-sum'
  | 'fast-slow'
  | 'tree-dfs'
  | 'graph-dfs'
  | 'backtracking'
  | 'bfs'
  | 'topological-sort'
  | 'dijkstra'
  | 'limited-shortest-path'
  | 'heap'
  | 'dp-1d'
  | 'dp-grid'
  | 'dp-dual'
  | 'dp-general'
  | 'memoization'
  | 'union-find'
  | 'trie'
  | 'segment-tree'
  | 'intervals'
  | 'stack'
  | 'monotonic-stack'
  | 'monotonic-deque'
  | 'divide-conquer'
  | 'line-sweep'
  | 'greedy'
  | 'math'
  | 'matrix'
  | 'string-processing'
  | 'string-matching'
  | 'eulerian-circuit'
  | 'quickselect'
  | 'counting'
  | 'linked-list'

export interface PatternProfile {
  id: PatternId
  title: string
  recognition: string
  state: string
  invariant: string
  transition: string
  correctness: string
  bottleneck: string
  edgeCase: string
  tradeoff: string
  time: string
  timeReason: string
  space: string
  spaceReason: string
  distractors: [PatternId, PatternId, PatternId]
}

const profiles: PatternProfile[] = [
  {
    id: 'hashing',
    title: 'Hash-based lookup',
    recognition: 'The solution repeatedly needs membership, a count, or an earlier index keyed by a value.',
    state: 'A hash map or hash set containing only the information needed for later constant-time expected lookups.',
    invariant: 'Before each lookup, the table represents exactly the portion of the input that the chosen scan has already processed.',
    transition: 'Query the needed key, use the result if present, and then update the table in the order required to avoid using invalid state.',
    correctness: 'Every candidate partner or prior state is either already represented by its key or is added when it becomes eligible.',
    bottleneck: 'A direct approach repeats a linear search for information that can instead be indexed once.',
    edgeCase: 'Duplicates and update order matter because inserting too early can allow an item to match itself.',
    tradeoff: 'Hashing usually improves expected lookup time by using additional memory and giving up sorted order.',
    time: 'O(n)',
    timeReason: 'The input is scanned once and each hash-table operation is expected O(1).',
    space: 'O(n)',
    spaceReason: 'The table can contain a linear number of distinct keys or processed values.',
    distractors: ['sorting', 'two-pointers-opposite', 'heap'],
  },
  {
    id: 'sorting',
    title: 'Sorting followed by a scan',
    recognition: 'Putting values into order makes comparisons, grouping, or later decisions local.',
    state: 'The sorted sequence plus a constant-size scan state, unless the sort itself requires auxiliary storage.',
    invariant: 'Everything before the scan position is already in final sorted order or has been completely grouped and processed.',
    transition: 'Sort by the key required by the proof, then advance through adjacent or ordered candidates once.',
    correctness: 'Sorting places values that must be compared near one another and makes skipped earlier positions permanently irrelevant.',
    bottleneck: 'Without ordering, the direct approach repeatedly searches the unsorted remainder for the next comparable item.',
    edgeCase: 'Equal keys require a deliberate tie rule so duplicates are grouped or ordered consistently.',
    tradeoff: 'Sorting costs O(n log n) but often simplifies later work to one linear scan.',
    time: 'O(n log n)',
    timeReason: 'Comparison sorting dominates the linear post-processing scan.',
    space: 'O(n)',
    spaceReason: 'The bound assumes an auxiliary array or language sort with linear worst-case working storage.',
    distractors: ['hashing', 'counting', 'binary-search'],
  },
  {
    id: 'simulation',
    title: 'Direct simulation',
    recognition: 'The problem defines deterministic rules that can be applied in order without exploring alternative states.',
    state: 'A compact representation of the current simulated state and any output accumulated so far.',
    invariant: 'Before each simulated step, the stored state exactly matches the result of applying all earlier rules.',
    transition: 'Apply the next rule once, update every affected part of the state consistently, and continue.',
    correctness: 'Induction over the steps shows that faithfully applying each rule produces the specified final state.',
    bottleneck: 'The main risk is not asymptotic search but inconsistent or duplicated state updates.',
    edgeCase: 'Boundary transitions and the final partially processed state must follow the same rules as ordinary steps.',
    tradeoff: 'Simulation is usually the clearest solution when the number of mandated steps already fits the constraints.',
    time: 'O(n)',
    timeReason: 'Each input item or required simulation step is processed a constant number of times.',
    space: 'O(n)',
    spaceReason: 'The bound allows output or explicit simulated state to grow linearly with the input.',
    distractors: ['backtracking', 'dp-general', 'sorting'],
  },
  {
    id: 'binary-search',
    title: 'Binary search',
    recognition: 'The candidates have a sorted order or a monotonic predicate that lets one comparison discard a whole half.',
    state: 'Two boundaries describing the exact remaining search interval.',
    invariant: 'If an answer exists, it remains inside the interval represented by the current boundaries.',
    transition: 'Test the midpoint and move exactly the boundary whose half has been proven unable to contain the answer.',
    correctness: 'Monotonicity makes the discarded half impossible, while the retained half preserves every feasible answer.',
    bottleneck: 'A linear scan examines candidates that sorted or monotonic structure can eliminate in groups.',
    edgeCase: 'Inclusive and exclusive boundary conventions must not be mixed, especially for a one-element interval.',
    tradeoff: 'Binary search gives logarithmic decisions only when midpoint testing really separates the remaining candidates monotonically.',
    time: 'O(log n)',
    timeReason: 'Each comparison reduces the remaining candidate range by about half.',
    space: 'O(1)',
    spaceReason: 'The iterative form stores only a constant number of indexes or numeric boundaries.',
    distractors: ['two-pointers-opposite', 'sorting', 'hashing'],
  },
  {
    id: 'two-pointers-same',
    title: 'Same-direction two pointers',
    recognition: 'One position scans while another marks a write position, matched prefix, or fixed gap in the same sequence.',
    state: 'A read pointer and a write, slow, or leading pointer with clearly different meanings.',
    invariant: 'Everything before the write or slow pointer already satisfies the required compacted or matched property.',
    transition: 'Advance the reader every iteration and advance the second pointer only when its invariant-preserving condition is met.',
    correctness: 'Each element is considered once and eligible elements are written or matched in their original relative order.',
    bottleneck: 'Repeated shifting or rescanning can be replaced by maintaining a frontier between processed and unprocessed data.',
    edgeCase: 'The second pointer must not advance when no valid item was written or matched.',
    tradeoff: 'The method uses constant extra space but may overwrite the input when used for in-place compaction.',
    time: 'O(n)',
    timeReason: 'Each pointer moves forward at most n positions.',
    space: 'O(1)',
    spaceReason: 'Only a constant number of indexes or node references is stored.',
    distractors: ['two-pointers-opposite', 'sliding-window-variable', 'hashing'],
  },
  {
    id: 'two-pointers-opposite',
    title: 'Opposite-direction two pointers',
    recognition: 'A pair or boundary decision can be made from the two ends of an ordered or symmetric search space.',
    state: 'Left and right boundaries representing the only pair range that can still improve or satisfy the answer.',
    invariant: 'Every pair using a position outside the current boundaries has already been evaluated or safely eliminated.',
    transition: 'Evaluate the endpoints and move only the side whose value proves that side cannot participate in a better remaining pair.',
    correctness: 'Order or the limiting-boundary argument proves that the moved endpoint cannot produce a missing optimal answer.',
    bottleneck: 'Enumerating every pair repeats comparisons that ordering can eliminate one full row or column at a time.',
    edgeCase: 'Moving both pointers without proof can skip the only valid pair.',
    tradeoff: 'The scan is linear and constant-space, but pair-sum variants require sorted input or a separate ordering step.',
    time: 'O(n)',
    timeReason: 'The two boundaries move inward a total of at most n positions.',
    space: 'O(1)',
    spaceReason: 'Only endpoint indexes and a constant-size answer state are maintained.',
    distractors: ['two-pointers-same', 'binary-search', 'hashing'],
  },
  {
    id: 'sliding-window-variable',
    title: 'Variable-size sliding window',
    recognition: 'The answer is a longest or shortest contiguous range whose validity can be updated as its boundaries move.',
    state: 'Left and right boundaries plus counts, indexes, or an aggregate describing only the current window.',
    invariant: 'After shrinking finishes, the current window satisfies the required validity condition.',
    transition: 'Add the new right element, then move left while the invariant is violated or while a valid window can still be improved.',
    correctness: 'Each boundary moves monotonically, and every maximal or minimal candidate is considered when its right endpoint is reached.',
    bottleneck: 'Rebuilding or rechecking every contiguous range repeats nearly all work between overlapping windows.',
    edgeCase: 'Negative values can break sum-based monotonicity, and stale counts must be removed when left advances.',
    tradeoff: 'Sliding windows give linear time only when validity can be maintained monotonically from boundary updates.',
    time: 'O(n)',
    timeReason: 'Each element enters and leaves the window at most once.',
    space: 'O(n)',
    spaceReason: 'The window state may store one entry per distinct value or character in the worst case.',
    distractors: ['prefix-sum', 'two-pointers-opposite', 'dp-1d'],
  },
  {
    id: 'sliding-window-fixed',
    title: 'Fixed-size sliding window',
    recognition: 'The problem asks about every contiguous block of exactly k elements.',
    state: 'A length-k window and an aggregate that can be updated when one item enters and one leaves.',
    invariant: 'Whenever a result is recorded, the maintained aggregate represents exactly the current k elements.',
    transition: 'Add the rightmost item, remove the item that falls k positions behind, and evaluate once the window is full.',
    correctness: 'Every length-k range appears exactly once as the right boundary advances.',
    bottleneck: 'Recomputing an aggregate independently for each window repeats k-1 elements of work.',
    edgeCase: 'The first result is available only when the right index reaches k-1, and the outgoing index must be right-k.',
    tradeoff: 'Incremental aggregates are simple, but max/min queries may require a monotonic deque rather than a scalar.',
    time: 'O(n)',
    timeReason: 'Every element is added once and removed at most once.',
    space: 'O(1)',
    spaceReason: 'A scalar aggregate requires constant auxiliary storage; richer frequency state may require more.',
    distractors: ['sliding-window-variable', 'prefix-sum', 'monotonic-deque'],
  },
  {
    id: 'prefix-sum',
    title: 'Prefix accumulation',
    recognition: 'A range result can be written as the difference between two cumulative states.',
    state: 'A prefix array or a map from previously seen prefix values to counts or positions.',
    invariant: 'The prefix at position i summarizes exactly the elements before i under the chosen cumulative operation.',
    transition: 'Extend the cumulative value with the next element and answer a range by subtracting the earlier boundary state.',
    correctness: 'Subtracting the common prefix cancels every element outside the requested contiguous range.',
    bottleneck: 'Summing each requested range independently repeats additions over shared prefixes.',
    edgeCase: 'An initial zero prefix is required so ranges beginning at index zero use the same formula.',
    tradeoff: 'Prefix sums make static range queries cheap but do not handle frequent point updates as well as a tree structure.',
    time: 'O(n)',
    timeReason: 'One pass builds or consumes the prefix state.',
    space: 'O(n)',
    spaceReason: 'A prefix entry or prefix-frequency entry may be stored for every input position.',
    distractors: ['sliding-window-variable', 'segment-tree', 'hashing'],
  },
  {
    id: 'fast-slow',
    title: 'Fast and slow pointers',
    recognition: 'A linked or functional sequence asks for a cycle, midpoint, or position relative to the end.',
    state: 'Two node references moving at different speeds or with a deliberate fixed gap.',
    invariant: 'Both pointers follow the same deterministic next relation while their speed or separation encodes the required distance.',
    transition: 'Advance slow by one and fast by two for cycle or midpoint logic, or preserve a fixed lead for end-relative positions.',
    correctness: 'Relative motion forces a meeting inside a cycle, while distance preservation locates midpoint or end-relative nodes.',
    bottleneck: 'Storing every visited node can be replaced by relative pointer motion.',
    edgeCase: 'Fast and fast.next must be checked before a two-step advance, and a meeting point is not automatically a cycle entrance.',
    tradeoff: 'The method uses constant memory but depends on a deterministic next relation.',
    time: 'O(n)',
    timeReason: 'The pointers traverse at most a constant multiple of the sequence length.',
    space: 'O(1)',
    spaceReason: 'Only a constant number of node references is stored.',
    distractors: ['hashing', 'two-pointers-same', 'bfs'],
  },
  {
    id: 'tree-dfs',
    title: 'Tree depth-first search',
    recognition: 'The result for a node can be computed from independently solved child subtrees.',
    state: 'The recursion stack plus the summary value returned by each child.',
    invariant: 'A completed dfs(node) call returns the documented, correct summary for the entire subtree rooted at node.',
    transition: 'Handle the null base case, solve the children, and combine their returned summaries for the parent.',
    correctness: 'Structural induction proves the base case and then proves every parent from correct child results.',
    bottleneck: 'Recomputing subtree properties from each ancestor repeats visits that one postorder traversal can share.',
    edgeCase: 'A skewed tree can make recursion depth linear, and BST validation may require ancestor bounds rather than parent checks.',
    tradeoff: 'Recursive DFS closely matches tree structure, while an explicit stack avoids call-stack limits.',
    time: 'O(n)',
    timeReason: 'Each tree node is entered and combined a constant number of times.',
    space: 'O(h)',
    spaceReason: 'The recursion or explicit stack stores at most one root-to-leaf path of height h.',
    distractors: ['bfs', 'graph-dfs', 'topological-sort'],
  },
  {
    id: 'graph-dfs',
    title: 'Graph depth-first search',
    recognition: 'The problem asks for reachability, components, or a property that can be explored one branch at a time.',
    state: 'A visited set and a recursion stack or explicit stack of vertices still being explored.',
    invariant: 'Every visited vertex has been reached by a valid path from the current traversal source.',
    transition: 'Mark a vertex when it is discovered and recursively or iteratively visit each unseen valid neighbor.',
    correctness: 'Following every outgoing edge from every reached vertex discovers exactly the source’s reachable component.',
    bottleneck: 'Starting an unrestricted search repeatedly from many vertices revisits the same component.',
    edgeCase: 'Cycles require marking before recursive expansion, and directed edges must not be treated as symmetric.',
    tradeoff: 'DFS is memory-efficient on narrow graphs but does not guarantee the fewest unweighted edges.',
    time: 'O(V + E)',
    timeReason: 'Every vertex is visited once and every adjacency-list edge is examined once.',
    space: 'O(V)',
    spaceReason: 'Visited state and the traversal stack can each contain a linear number of vertices.',
    distractors: ['bfs', 'union-find', 'topological-sort'],
  },
  {
    id: 'backtracking',
    title: 'Backtracking search',
    recognition: 'The task asks to construct or enumerate valid combinations, permutations, partitions, or assignments.',
    state: 'A mutable partial candidate and the decisions that remain available at the current depth.',
    invariant: 'The partial candidate satisfies every constraint that can already be checked.',
    transition: 'Choose one candidate, recurse, then undo the choice before trying the next candidate.',
    correctness: 'The decision tree contains every legal construction, and pruning removes only prefixes that cannot become valid solutions.',
    bottleneck: 'Blind enumeration explores invalid branches long after a constraint has already failed.',
    edgeCase: 'Mutable state must be undone and recorded solutions must be copied; duplicates require same-depth deduplication.',
    tradeoff: 'Pruning can greatly reduce practical work, but worst-case growth remains exponential in the decision depth.',
    time: 'O(b^d)',
    timeReason: 'A decision tree of depth d may branch into as many as b choices per level, excluding output-copy cost.',
    space: 'O(d)',
    spaceReason: 'The active recursion path and partial candidate grow with the decision depth d, excluding returned output.',
    distractors: ['dp-general', 'graph-dfs', 'greedy'],
  },
  {
    id: 'bfs',
    title: 'Breadth-first search',
    recognition: 'The problem asks for level order or the fewest equal-cost transitions in an unweighted state graph.',
    state: 'A FIFO queue for the frontier and visited state recorded at discovery time.',
    invariant: 'Vertices leave the queue in nondecreasing distance from the source.',
    transition: 'Dequeue the next vertex, discover each unseen neighbor, mark it immediately, and enqueue it once.',
    correctness: 'All distance-d vertices are expanded before any distance-(d+1) vertex, so first discovery uses the fewest edges.',
    bottleneck: 'Searching every possible path repeats prefixes; BFS merges paths that reach the same state.',
    edgeCase: 'Marking visited only when dequeued can enqueue a vertex many times; unequal weights invalidate ordinary BFS shortest paths.',
    tradeoff: 'BFS guarantees fewest unweighted steps but its frontier can be much wider than a DFS stack.',
    time: 'O(V + E)',
    timeReason: 'Each vertex is enqueued once and every outgoing edge is examined once.',
    space: 'O(V)',
    spaceReason: 'The queue and visited set can each contain a linear number of vertices.',
    distractors: ['graph-dfs', 'dijkstra', 'topological-sort'],
  },
  {
    id: 'topological-sort',
    title: 'Topological sorting',
    recognition: 'Directed prerequisite relationships require a valid dependency order or directed-cycle detection.',
    state: 'An adjacency list, an indegree count for every vertex, and a queue of currently zero-indegree vertices.',
    invariant: 'Every queued vertex has no remaining prerequisite among the vertices not yet processed.',
    transition: 'Remove one zero-indegree vertex and decrement the indegree of each dependent vertex.',
    correctness: 'A zero-indegree vertex can safely appear next; processing fewer than V vertices proves that a directed cycle blocks an order.',
    bottleneck: 'Repeatedly scanning all dependencies for an available task duplicates work that indegree counts maintain incrementally.',
    edgeCase: 'Reversing edge direction reverses indegree meaning, and a partial result must not be returned as a complete order.',
    tradeoff: 'Kahn’s algorithm naturally detects cycles; DFS postorder is an alternative but needs explicit visiting-state cycle checks.',
    time: 'O(V + E)',
    timeReason: 'Each vertex is queued once and each dependency edge decrements one indegree once.',
    space: 'O(V + E)',
    spaceReason: 'The adjacency representation, indegree array, queue, and result store the graph and its vertices.',
    distractors: ['bfs', 'dijkstra', 'union-find'],
  },
  {
    id: 'dijkstra',
    title: 'Dijkstra’s shortest path',
    recognition: 'The objective is minimum total path cost and every edge weight is non-negative.',
    state: 'A distance array or map and a min-heap of tentative distance/vertex entries.',
    invariant: 'When a non-stale minimum-distance entry is removed from the heap, no undiscovered route can improve that distance.',
    transition: 'Relax every outgoing edge and push a new heap entry whenever a shorter tentative distance is found.',
    correctness: 'Non-negative weights ensure any route discovered later through a more expensive frontier cannot improve a finalized minimum.',
    bottleneck: 'Repeatedly scanning all unvisited vertices for the smallest tentative distance is avoided by the heap.',
    edgeCase: 'Negative weights break the proof, and stale heap entries must be skipped rather than treated as current distances.',
    tradeoff: 'Dijkstra is efficient for non-negative weights; Bellman-Ford-style relaxation is needed when negative edges or edge limits matter.',
    time: 'O((V + E) log V)',
    timeReason: 'Each successful relaxation pushes a heap entry and heap operations cost logarithmic time.',
    space: 'O(V + E)',
    spaceReason: 'The graph, distance table, and heap together use linear graph storage.',
    distractors: ['bfs', 'limited-shortest-path', 'topological-sort'],
  },
  {
    id: 'limited-shortest-path',
    title: 'Bounded-edge shortest-path relaxation',
    recognition: 'A cheapest path is constrained by a maximum number of edges, stops, or rounds of transitions.',
    state: 'A distance table representing the best cost achievable with at most the previous number of allowed edges.',
    invariant: 'After round k, each stored distance uses no more than k edges from the source.',
    transition: 'Relax every edge from a snapshot of the previous round so one round cannot consume multiple new edges.',
    correctness: 'Induction on the allowed edge count considers every path of permitted length without admitting a longer one early.',
    bottleneck: 'Ordinary shortest-path finalization does not encode the stop count and can discard a costlier prefix that uses fewer edges.',
    edgeCase: 'Updating the same distance array in place can accidentally use more than one new edge during a round.',
    tradeoff: 'Layered relaxation handles edge limits directly but may cost one full edge scan per allowed step.',
    time: 'O(kE)',
    timeReason: 'Each of k allowed-edge rounds examines every edge once.',
    space: 'O(V)',
    spaceReason: 'Rolling previous and current distance arrays store one value per vertex.',
    distractors: ['dijkstra', 'bfs', 'dp-general'],
  },
  {
    id: 'heap',
    title: 'Heap / priority queue',
    recognition: 'The algorithm repeatedly needs only the smallest, largest, or next-best remaining candidate.',
    state: 'A min-heap or max-heap containing exactly the active candidates that may be selected next.',
    invariant: 'The heap root is the best active candidate under the chosen ordering.',
    transition: 'Push newly eligible candidates, remove obsolete ones when required, and pop the root when the next best item is consumed.',
    correctness: 'The heap property guarantees that no stored active candidate ranks ahead of the root.',
    bottleneck: 'Repeatedly sorting or scanning every remaining candidate performs unnecessary work after each update.',
    edgeCase: 'Min versus max ordering must match the retained set, and stale or expired entries need explicit removal rules.',
    tradeoff: 'A heap avoids full sorting when only repeated extrema are needed, but arbitrary search remains linear.',
    time: 'O(n log k)',
    timeReason: 'A typical bounded heap processes n items with O(log k) insertion or removal.',
    space: 'O(k)',
    spaceReason: 'A bounded top-k or k-way frontier stores at most k active entries.',
    distractors: ['sorting', 'quickselect', 'monotonic-deque'],
  },
  {
    id: 'dp-1d',
    title: 'One-dimensional dynamic programming',
    recognition: 'The result at position i depends on a fixed number of earlier positions or take/skip choices.',
    state: 'A dp value for each prefix or position, often reducible to a few rolling values.',
    invariant: 'Before computing dp[i], every state required by its recurrence already stores the optimal answer for its defined subproblem.',
    transition: 'Combine the constant-size set of predecessor states specified by the recurrence.',
    correctness: 'Optimal substructure means every solution ends with one represented predecessor choice, and the recurrence selects the best.',
    bottleneck: 'Naive recursion recomputes the same suffix or prefix states exponentially many times.',
    edgeCase: 'Base cases for empty and one-item inputs determine every later state and must match the state definition.',
    tradeoff: 'Rolling state reduces memory only after the full recurrence and update order are established.',
    time: 'O(n)',
    timeReason: 'There are n states and each uses a constant number of transitions.',
    space: 'O(n)',
    spaceReason: 'The direct formulation stores one result per input position; some problems can reduce this to O(1).',
    distractors: ['greedy', 'backtracking', 'dp-general'],
  },
  {
    id: 'dp-grid',
    title: 'Grid dynamic programming',
    recognition: 'Each matrix cell’s result depends on a fixed set of neighboring cells that can precede it.',
    state: 'A dp cell representing the count, minimum, maximum, or feasibility for reaching that location.',
    invariant: 'At each cell, every predecessor permitted by the movement rules has already been computed.',
    transition: 'Combine the valid predecessor cells with the current cell’s cost, obstacle, or contribution.',
    correctness: 'Every valid path into a cell must end at one of the enumerated predecessors, so the recurrence covers all possibilities.',
    bottleneck: 'Unmemoized path recursion recomputes the same cell from many different prefixes.',
    edgeCase: 'The first row, first column, obstacles, and impossible states need explicit initialization.',
    tradeoff: 'Row-by-row dependencies often allow O(columns) memory instead of storing the complete grid table.',
    time: 'O(rows * columns)',
    timeReason: 'Each grid cell is computed once from a constant number of neighbors.',
    space: 'O(rows * columns)',
    spaceReason: 'The direct table stores one result per cell; rolling rows can reduce this for compatible recurrences.',
    distractors: ['bfs', 'matrix', 'dp-1d'],
  },
  {
    id: 'dp-dual',
    title: 'Dual-sequence dynamic programming',
    recognition: 'The problem aligns, compares, or transforms prefixes of two strings or arrays.',
    state: 'dp[i][j] represents the complete answer for a precisely defined pair of prefixes.',
    invariant: 'When dp[i][j] is computed, every smaller prefix pair referenced by its recurrence is final.',
    transition: 'Move diagonally when current elements match, or combine allowed skip, insert, delete, or replace transitions when they differ.',
    correctness: 'Every valid alignment ends in one of the recurrence’s final operations, so considering each operation is exhaustive.',
    bottleneck: 'Recursive alignment branches revisit the same pair of prefix positions.',
    edgeCase: 'The empty-prefix row and column encode insertions, deletions, or zero matches and must follow the state definition.',
    tradeoff: 'The O(mn) table is broadly applicable; rolling rows reduce space when reconstruction is not required.',
    time: 'O(mn)',
    timeReason: 'There are m by n prefix-pair states with constant transition work.',
    space: 'O(mn)',
    spaceReason: 'The direct formulation stores one value for every pair of prefix lengths.',
    distractors: ['string-matching', 'sliding-window-variable', 'dp-1d'],
  },
  {
    id: 'dp-general', title: 'Dynamic programming',
    recognition: 'The problem has overlapping subproblems and an optimal answer that can be composed from smaller states.',
    state: 'A table or memo whose key contains exactly the information needed to determine the remaining answer.',
    invariant: 'Every stored state equals the correct answer for the subproblem named by its key.',
    transition: 'Enumerate every legal next decision and combine it with already solved successor or predecessor states.',
    correctness: 'The state definition partitions all valid solutions by their next or final decision, so the recurrence is exhaustive.',
    bottleneck: 'A recursive search repeats identical subproblems unless their results are stored.',
    edgeCase: 'An incomplete state key can merge subproblems that have different legal futures.',
    tradeoff: 'Dynamic programming exchanges memory and state-design effort for eliminating repeated computation.',
    time: 'O(number of states * transitions per state)', timeReason: 'Each distinct state is solved once and examines its legal transitions.',
    space: 'O(number of states)', spaceReason: 'The memo or table stores one result per distinct state.',
    distractors: ['greedy', 'backtracking', 'memoization'],
  },
  {
    id: 'memoization', title: 'Memoized search',
    recognition: 'A natural recursive search reaches the same state through multiple decision sequences.',
    state: 'A cache keyed by the complete recursive state.', invariant: 'A cached value is the final answer for exactly its key.',
    transition: 'Return cached results; otherwise solve all legal child states and cache their combination.',
    correctness: 'Caching changes neither the recurrence nor its choices; it only reuses previously proved state results.',
    bottleneck: 'Without caching, the recursion tree contains exponentially many repeated nodes.',
    edgeCase: 'Cache lookup must distinguish an absent entry from a valid false, zero, or empty result.',
    tradeoff: 'Top-down memoization visits only reachable states but adds recursion overhead and stack depth.',
    time: 'O(number of reachable states * transitions per state)', timeReason: 'Each reachable state is evaluated once.',
    space: 'O(number of reachable states)', spaceReason: 'The cache and recursion stack scale with reachable states and depth.',
    distractors: ['backtracking', 'dp-general', 'greedy'],
  },
  {
    id: 'union-find', title: 'Disjoint-set union',
    recognition: 'Edges arrive over time and queries ask whether vertices belong to the same connected component.',
    state: 'Parent pointers and component ranks or sizes representing a forest of sets.',
    invariant: 'Two elements have the same representative exactly when they are in the same maintained component.',
    transition: 'Find both representatives, then union different roots using rank or size and path compression.',
    correctness: 'Union mirrors addition of a connecting edge, while find returns the unique representative of the resulting set.',
    bottleneck: 'Re-running a full graph traversal after every added edge repeats connectivity work.',
    edgeCase: 'An edge whose endpoints already share a representative creates a cycle rather than merging components.',
    tradeoff: 'Union-find supports fast connectivity updates but does not recover paths or support arbitrary edge deletion.',
    time: 'O((V + E) alpha(V))', timeReason: 'Path compression and union by rank give amortized inverse-Ackermann operations.',
    space: 'O(V)', spaceReason: 'Parent and rank or size arrays contain one entry per vertex.',
    distractors: ['graph-dfs', 'bfs', 'topological-sort'],
  },
  {
    id: 'trie', title: 'Trie', recognition: 'Queries repeatedly share and inspect string prefixes.',
    state: 'A rooted character tree whose path spells a prefix and whose terminal marker identifies a complete word.',
    invariant: 'Following characters from the root succeeds exactly for prefixes inserted into the trie.',
    transition: 'Advance or create one child per character; mark the final node for complete-word membership.',
    correctness: 'Each path consumes characters in order, so reaching a terminal node is equivalent to matching the full word.',
    bottleneck: 'Scanning every stored word repeats comparison of shared prefixes.',
    edgeCase: 'A prefix node is not necessarily a complete word; terminal markers must be checked separately.',
    tradeoff: 'Tries make prefix operations proportional to query length but may use much more memory than a hash set.',
    time: 'O(L)', timeReason: 'Insertion or lookup follows one edge for each of L characters.',
    space: 'O(total stored characters)', spaceReason: 'Nodes represent distinct stored prefixes.',
    distractors: ['hashing', 'string-matching', 'tree-dfs'],
  },
  {
    id: 'segment-tree', title: 'Segment tree', recognition: 'The input changes while range aggregates must be queried repeatedly.',
    state: 'A balanced tree in which each node stores the aggregate for one array interval.',
    invariant: 'Every node value equals the combination of its children over exactly its represented interval.',
    transition: 'Update one root-to-leaf path or decompose a query range into disjoint represented intervals.',
    correctness: 'The selected nodes partition the requested range, and the associative combine operation preserves its aggregate.',
    bottleneck: 'Recomputing an entire range after each point update costs linear time.',
    edgeCase: 'Interval boundaries and half-open versus closed conventions must remain consistent.',
    tradeoff: 'A segment tree supports general associative aggregates but uses more code and memory than prefix sums.',
    time: 'O(log n) per update or query', timeReason: 'A balanced tree visits only logarithmically many levels and canonical intervals.',
    space: 'O(n)', spaceReason: 'The tree contains a linear number of aggregate nodes.',
    distractors: ['prefix-sum', 'binary-search', 'divide-conquer'],
  },
  {
    id: 'intervals', title: 'Sorted interval processing', recognition: 'The input consists of ranges whose overlap, coverage, or ordering matters.',
    state: 'Intervals sorted by a relevant boundary plus the active merged interval or event state.',
    invariant: 'All processed intervals are resolved, and the active state summarizes the only unresolved overlap with the suffix.',
    transition: 'Compare the next interval with the active boundary; merge, emit, count, or replace as required.',
    correctness: 'Sorting ensures no later interval can start before the one currently considered.',
    bottleneck: 'Comparing every pair of intervals costs quadratic time.',
    edgeCase: 'Touching endpoints may or may not overlap depending on whether intervals are closed or half-open.',
    tradeoff: 'Sorting enables one-pass processing but determines an O(n log n) lower-order cost when input is unsorted.',
    time: 'O(n log n)', timeReason: 'Sorting dominates the subsequent linear sweep.', space: 'O(n)', spaceReason: 'The sorted or merged output can require linear storage.',
    distractors: ['sorting', 'line-sweep', 'heap'],
  },
  {
    id: 'stack', title: 'Stack', recognition: 'The most recently opened, encountered, or deferred item must be resolved first.',
    state: 'A LIFO stack of unresolved items.', invariant: 'The stack contains unresolved items in the only order in which they may legally close or resume.',
    transition: 'Push newly opened work and pop when the current input resolves the top item.',
    correctness: 'LIFO order matches nested structure: an inner item must finish before its enclosing item.',
    bottleneck: 'Searching backward for the matching unresolved item repeats work and obscures nesting.',
    edgeCase: 'A closing item with an empty stack, or a nonempty stack after the scan, is invalid.',
    tradeoff: 'A stack gives constant-time access to the newest item but cannot directly access older unresolved items.',
    time: 'O(n)', timeReason: 'Each item is pushed and popped at most once.', space: 'O(n)', spaceReason: 'All items may remain unresolved simultaneously.',
    distractors: ['bfs', 'hashing', 'monotonic-stack'],
  },
  {
    id: 'monotonic-stack', title: 'Monotonic stack', recognition: 'Each element needs the next or previous greater or smaller element.',
    state: 'Indices whose values remain monotonic and have not yet found their resolving neighbor.',
    invariant: 'Stack order is monotonic, and every retained index is unresolved by all elements scanned so far.',
    transition: 'Pop while the current value resolves the top, record answers, then push the current index.',
    correctness: 'The first value that violates monotonic order is exactly the nearest qualifying resolver for every popped index.',
    bottleneck: 'Scanning outward independently from every index can examine the same range many times.',
    edgeCase: 'Equal values require a deliberate strict or non-strict comparison.',
    tradeoff: 'The stack gives linear nearest-neighbor processing but applies only when resolution is monotonic.',
    time: 'O(n)', timeReason: 'Each index is pushed and popped at most once.', space: 'O(n)', spaceReason: 'A monotonic input can leave every index on the stack.',
    distractors: ['stack', 'two-pointers-same', 'sorting'],
  },
  {
    id: 'monotonic-deque', title: 'Monotonic deque', recognition: 'A moving window needs its minimum or maximum after each one-step update.',
    state: 'A deque of in-window indices ordered by position and monotonically by value.',
    invariant: 'The front is the window optimum; dominated indices are removed because they can never become optimal later.',
    transition: 'Expire front indices, remove dominated back indices, append the new index, and read the front.',
    correctness: 'Every removed value is expired or dominated by a newer value that remains available at least as long.',
    bottleneck: 'Computing every window maximum independently costs O(nk).', edgeCase: 'Store indices, not only values, so expired duplicates can be identified.',
    tradeoff: 'A monotonic deque gives O(1) amortized extrema but not arbitrary order statistics.',
    time: 'O(n)', timeReason: 'Each index enters and leaves the deque at most once.', space: 'O(k)', spaceReason: 'Only indices from the current window are retained.',
    distractors: ['heap', 'sliding-window-fixed', 'monotonic-stack'],
  },
  {
    id: 'divide-conquer', title: 'Divide and conquer', recognition: 'The problem splits into smaller independent regions whose results can be combined.',
    state: 'A subproblem boundary together with the result returned for that region.', invariant: 'Each recursive call returns a correct result for exactly its assigned region.',
    transition: 'Choose a split, solve each smaller region, then combine their results.', correctness: 'The split covers the original region and the combine step accounts for all cross-boundary effects.',
    bottleneck: 'A direct global computation may repeatedly solve the same structural work at multiple scales.', edgeCase: 'Base cases must stop on empty or single-element regions.',
    tradeoff: 'Divide and conquer enables clean recursion and sometimes parallelism, but combine costs determine whether it is efficient.',
    time: 'O(n log n)', timeReason: 'A common balanced recurrence has logarithmic levels with linear work per level.', space: 'O(log n)', spaceReason: 'Balanced recursion uses logarithmic call depth, excluding output or combine buffers.',
    distractors: ['binary-search', 'backtracking', 'dp-general'],
  },
  {
    id: 'line-sweep', title: 'Line sweep', recognition: 'The answer changes only at ordered interval or geometric events.',
    state: 'Sorted events and an active count or structure describing the region at the current coordinate.', invariant: 'Before the next event, active state exactly represents every object covering the open region just swept.',
    transition: 'Advance to the next coordinate, account for the previous span, then apply all events at that coordinate.', correctness: 'Between consecutive events no membership changes, so one state correctly describes the entire span.',
    bottleneck: 'Checking every coordinate or every pair ignores that state changes only at event boundaries.', edgeCase: 'Events sharing a coordinate need a consistent tie rule or batching.',
    tradeoff: 'Line sweep compresses a continuous domain into events but may require a secondary ordered structure.',
    time: 'O(n log n)', timeReason: 'Sorting the events dominates a linear or logarithmic-update sweep.', space: 'O(n)', spaceReason: 'The event list and active structure may both be linear.',
    distractors: ['intervals', 'sorting', 'heap'],
  },
  {
    id: 'greedy', title: 'Greedy choice', recognition: 'A locally best eligible decision can be proven safe and never needs reversal.',
    state: 'The processed prefix and the best feasible summary needed for the next choice.', invariant: 'After each choice, some globally optimal solution is still consistent with all choices made so far.',
    transition: 'Select the locally preferred feasible option and update the summary.', correctness: 'An exchange or staying-ahead argument replaces an optimal solution’s next choice with the greedy one without worsening it.',
    bottleneck: 'Exploring all decision combinations is unnecessary once the greedy-choice property is proved.', edgeCase: 'A plausible local rule is not sufficient; counterexamples must be ruled out by proof.',
    tradeoff: 'Greedy solutions are small and fast, but only correct when the required exchange invariant holds.',
    time: 'O(n)', timeReason: 'After any required ordering, a typical greedy scan processes each item once.', space: 'O(1)', spaceReason: 'Many greedy scans keep only a constant-size summary.',
    distractors: ['dp-general', 'sorting', 'backtracking'],
  },
  {
    id: 'math', title: 'Mathematical derivation', recognition: 'The result follows from algebraic, combinatorial, geometric, or number-theoretic structure rather than state search.',
    state: 'A small set of derived quantities or accumulators justified by the formula.', invariant: 'Each accumulator retains its precise mathematical meaning after every processed term.',
    transition: 'Apply the derived recurrence or identity to incorporate the next term.', correctness: 'The algorithm is a direct evaluation of the proved identity under the problem constraints.',
    bottleneck: 'Enumerating all objects represented by a closed form or recurrence performs unnecessary work.', edgeCase: 'Overflow, integer division, signs, and modular arithmetic can invalidate an otherwise correct derivation.',
    tradeoff: 'A derivation can be optimal but is often less discoverable and more sensitive to numeric assumptions.',
    time: 'O(n)', timeReason: 'A typical derived scan incorporates each input value once.', space: 'O(1)', spaceReason: 'Only a constant number of mathematical accumulators is required.',
    distractors: ['simulation', 'dp-general', 'counting'],
  },
  {
    id: 'matrix', title: 'Matrix traversal', recognition: 'Cells must be visited or transformed according to row, column, diagonal, or boundary geometry.',
    state: 'Coordinates, boundaries, or visitation state that identify the processed portion of the matrix.', invariant: 'Every cell in the processed region has its final required value or visit status.',
    transition: 'Advance coordinates or shrink boundaries according to the traversal order.', correctness: 'The boundary rules partition the matrix into disjoint visits and cover every required cell.',
    bottleneck: 'Extra copying or repeated scans are avoidable when coordinate mappings are known.', edgeCase: 'Single rows, single columns, and odd center layers can otherwise be visited twice.',
    tradeoff: 'In-place traversal saves memory but requires careful ordering to avoid overwriting unread values.',
    time: 'O(rows * columns)', timeReason: 'Each matrix cell is processed a constant number of times.', space: 'O(1)', spaceReason: 'Boundary-based traversal can use only a constant number of indices.',
    distractors: ['simulation', 'dp-grid', 'bfs'],
  },
  {
    id: 'string-processing', title: 'String processing', recognition: 'Character order, token boundaries, or normalization rules drive a direct scan.',
    state: 'The current index plus the partial token, count, or output built from the processed prefix.', invariant: 'The accumulated state is the exact required transformation of the processed prefix.',
    transition: 'Classify the next character and update the current token or output according to the specification.', correctness: 'The scan applies the required rule to every character in order without changing relative order unless specified.',
    bottleneck: 'Repeated slicing or immutable concatenation can turn a linear scan into quadratic copying.', edgeCase: 'Empty strings, repeated separators, Unicode assumptions, and final unterminated tokens need explicit handling.',
    tradeoff: 'A direct scan is simple and linear, while more specialized matching structures are needed for repeated pattern queries.',
    time: 'O(n)', timeReason: 'Each character is examined a constant number of times.', space: 'O(n)', spaceReason: 'The transformed output or token buffer can be proportional to input length.',
    distractors: ['string-matching', 'sliding-window-variable', 'stack'],
  },
  {
    id: 'string-matching', title: 'Linear string matching', recognition: 'A pattern must be found in text while avoiding rechecking matched prefixes after a mismatch.',
    state: 'The current text position and the length of the pattern prefix matched so far, supported by a prefix table.', invariant: 'The matched length is the longest pattern prefix that is a suffix of the processed text prefix.',
    transition: 'On mismatch, fall back through the prefix table; on match, extend the matched prefix.', correctness: 'Fallback preserves every prefix that could still match and discards only prefixes contradicted by the mismatch.',
    bottleneck: 'Restarting from each text position can compare the same characters quadratically.', edgeCase: 'An empty pattern and overlapping matches require explicit API semantics.',
    tradeoff: 'Prefix preprocessing gives worst-case linear matching but is more complex than a simple scan for tiny inputs.',
    time: 'O(n + m)', timeReason: 'Text and pattern-prefix pointers advance or fall back a linear total number of times.', space: 'O(m)', spaceReason: 'The prefix table stores one value per pattern position.',
    distractors: ['string-processing', 'sliding-window-variable', 'trie'],
  },
  {
    id: 'eulerian-circuit', title: 'Eulerian path construction', recognition: 'Every directed edge must be used exactly once while producing a valid continuous route.',
    state: 'Per-vertex unused outgoing edges and a stack or recursive path whose dead ends are appended to the answer.', invariant: 'Removed edges are used exactly once, and the reversed output contains completed suffixes that need no remaining edge.',
    transition: 'Consume an unused outgoing edge; when none remains, append the vertex while backtracking.', correctness: 'Splicing completed cycles during backtracking yields a continuous route containing every edge exactly once.',
    bottleneck: 'Backtracking over permutations of edges is exponential.', edgeCase: 'Lexicographic requirements demand ordered adjacency and the graph must satisfy Eulerian degree/connectivity conditions.',
    tradeoff: 'Hierholzer’s algorithm is linear after adjacency ordering but applies specifically to Eulerian traversal.',
    time: 'O(E log E)', timeReason: 'Ordering adjacency costs O(E log E); each edge is then consumed once.', space: 'O(V + E)', spaceReason: 'Adjacency, traversal stack, and output store the graph and route.',
    distractors: ['graph-dfs', 'topological-sort', 'backtracking'],
  },
  {
    id: 'quickselect', title: 'Quickselect', recognition: 'Only the kth order statistic is needed, not the complete sorted order.',
    state: 'An active partition range known to contain the target rank.', invariant: 'Elements discarded from the active range cannot contain the requested rank after partitioning.',
    transition: 'Partition around a pivot and continue only in the side containing the target index.', correctness: 'Partition places the pivot at its final rank and separates every smaller and larger rank.',
    bottleneck: 'Fully sorting performs ordering work irrelevant to a single requested rank.', edgeCase: 'Poor deterministic pivots can cause quadratic worst-case time; duplicates require consistent partitioning.',
    tradeoff: 'Quickselect has linear expected time but mutates input and lacks the heap’s streaming guarantees.',
    time: 'O(n) expected', timeReason: 'Randomized partitioning discards a constant fraction in expectation.', space: 'O(1)', spaceReason: 'Iterative in-place partitioning needs only constant auxiliary storage.',
    distractors: ['sorting', 'heap', 'binary-search'],
  },
  {
    id: 'counting', title: 'Counting / frequency aggregation', recognition: 'Values lie in a manageable domain or the answer depends only on occurrence counts.',
    state: 'A frequency table and any aggregate derived from processed counts.', invariant: 'Each frequency equals the number of occurrences in the processed prefix.',
    transition: 'Increment the current value’s bucket and update dependent aggregates.', correctness: 'Every input occurrence contributes exactly once to its value’s frequency.',
    bottleneck: 'Comparison sorting is unnecessary when frequency order or counts alone determine the result.', edgeCase: 'The value range may be too large or sparse for an array and may require a map.',
    tradeoff: 'Counting can be linear but its memory depends on the value domain rather than only input length.',
    time: 'O(n + k)', timeReason: 'The input and k possible value buckets are each scanned once.', space: 'O(k)', spaceReason: 'One counter is stored for each possible or observed value.',
    distractors: ['hashing', 'sorting', 'math'],
  },
  {
    id: 'linked-list', title: 'Linked-list pointer manipulation', recognition: 'Nodes must be reordered, removed, merged, or traversed without random indexing.',
    state: 'A small set of node pointers naming the processed prefix, current node, and remaining suffix.', invariant: 'The processed portion has its final links and every unprocessed node remains reachable.',
    transition: 'Save the next pointer before rewiring current links, then advance the pointer roles.', correctness: 'Each step preserves reachability while extending the correctly linked processed portion by one node.',
    bottleneck: 'Copying values into an array discards the list’s O(1) link-update advantage.', edgeCase: 'Empty lists, one node, head replacement, and temporary cycles require dummy nodes or careful ordering.',
    tradeoff: 'Pointer rewiring uses constant extra memory but is less forgiving than rebuilding a new list.',
    time: 'O(n)', timeReason: 'Each node is visited a constant number of times.', space: 'O(1)', spaceReason: 'Iterative manipulation uses a constant number of pointers.',
    distractors: ['two-pointers-same', 'fast-slow', 'simulation'],
  },
]

export const patternProfiles = Object.fromEntries(profiles.map((profile) => [profile.id, profile])) as Record<PatternId, PatternProfile>

export const rawTagPatternMap: Record<string, PatternId> = {
  Array: 'simulation', Backtracking: 'backtracking', 'Binary Indexed Tree': 'segment-tree', 'Binary Search': 'binary-search',
  'Breadth-First Search': 'bfs', 'Bucket Sort': 'counting', Combinatorics: 'math', Counting: 'counting',
  'Depth-First Search': 'graph-dfs', 'Divide and Conquer': 'divide-conquer', 'Dynamic Programming': 'dp-general',
  'Eulerian Circuit': 'eulerian-circuit', Geometry: 'math', Greedy: 'greedy', 'Hash Table': 'hashing', Heap: 'heap',
  'Linked List': 'linked-list', Math: 'math', Matrix: 'matrix', Memoization: 'memoization', 'Merge Sort': 'sorting',
  'Monotonic Queue': 'monotonic-deque', 'Monotonic Stack': 'monotonic-stack', 'Ordered Set': 'sorting',
  'Prefix Sum': 'prefix-sum', Queue: 'bfs', Quickselect: 'quickselect', Recursion: 'backtracking',
  'Segment Tree': 'segment-tree', 'Shortest Path': 'dijkstra', Simulation: 'simulation', 'Sliding Window': 'sliding-window-variable',
  Sorting: 'sorting', Stack: 'stack', String: 'string-processing', 'String Matching': 'string-matching',
  'Topological Sort': 'topological-sort', Trie: 'trie', 'Two Pointers': 'two-pointers-same', 'Union Find': 'union-find',
  Hashing: 'hashing', 'One Pass': 'greedy',
}
