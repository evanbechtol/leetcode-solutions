import type { PatternId } from './patterns'

/**
 * Short, learner-facing descriptions used inside questions and walkthroughs.
 * The more formal PatternProfile remains the source for complexity and review.
 */
export interface BeginnerPatternProfile {
  clue: string
  memory: string
  promise: string
  step: string
  why: string
  repeatedWork: string
  watchOut: string
  tradeoff: string
}

export const beginnerPatternProfiles: Record<PatternId, BeginnerPatternProfile> = {
  hashing: {
    clue: 'Look up a value, count, or earlier position many times while scanning once.',
    memory: 'Keep a map or set containing values the scan may need again.',
    promise: 'The map or set exactly represents the part of the input already processed.',
    step: 'Check the needed key, use it if found, then store the current information.',
    why: 'Every earlier value becomes available exactly when a later step may need it.',
    repeatedWork: 'A basic solution searches earlier values again for every new value.',
    watchOut: 'Inserting too early can let an item incorrectly match itself.',
    tradeoff: 'Use extra memory to make repeated lookups fast on average.',
  },
  sorting: {
    clue: 'Put values in order so related items become neighbors.',
    memory: 'Keep the ordered values and a small amount of scan information.',
    promise: 'Everything before the scan position is already handled in sorted order.',
    step: 'Sort by the useful key, then process the ordered values from left to right.',
    why: 'Ordering brings values that must be compared close together.',
    repeatedWork: 'Without order, the solution repeatedly searches the remaining values.',
    watchOut: 'Equal values need a consistent rule for their order.',
    tradeoff: 'Spend time sorting to make the remaining decisions simpler.',
  },
  simulation: {
    clue: 'The problem gives rules that can be followed directly in order.',
    memory: 'Keep the current state and any result built so far.',
    promise: 'The stored state matches the result of every rule already applied.',
    step: 'Apply the next rule once and update every affected part of the state.',
    why: 'Following each rule correctly produces exactly the requested final state.',
    repeatedWork: 'The main danger is updating the same state twice or inconsistently.',
    watchOut: 'Boundary steps and the last partial step still need the same rules.',
    tradeoff: 'Direct simulation favors clarity when the required number of steps is affordable.',
  },
  'binary-search': {
    clue: 'One test can prove that an entire half cannot contain the answer.',
    memory: 'Keep left and right boundaries around the remaining candidates.',
    promise: 'If an answer exists, it is still between the two boundaries.',
    step: 'Test the middle candidate and discard only the half proven impossible.',
    why: 'Each test preserves every possible answer while shrinking the search range.',
    repeatedWork: 'A simple scan checks candidates that can be rejected together.',
    watchOut: 'Use one consistent rule for whether each boundary is included.',
    tradeoff: 'Get very fast search only when the test reliably separates the candidates.',
  },
  'two-pointers-same': {
    clue: 'One position reads while another tracks where accepted work belongs.',
    memory: 'Keep a reading position and a second position with a different job.',
    promise: 'Everything before the second position is already in its correct form.',
    step: 'Move the reader each time; move the second position only when appropriate.',
    why: 'Every item is considered once and accepted items keep the needed order.',
    repeatedWork: 'Repeated shifting can be replaced by one moving write boundary.',
    watchOut: 'Do not move the second position when nothing was accepted.',
    tradeoff: 'Use little extra memory, sometimes by changing the input in place.',
  },
  'two-pointers-opposite': {
    clue: 'A choice can be made by comparing the two ends of an ordered range.',
    memory: 'Keep left and right positions around the only candidates still possible.',
    promise: 'Anything outside the two positions has already been handled or ruled out.',
    step: 'Compare the ends and move the one proven unable to help.',
    why: 'The ordering proves that moving that end cannot skip the best answer.',
    repeatedWork: 'Checking every pair repeats comparisons that ordering can eliminate together.',
    watchOut: 'Moving both ends without a reason can skip the only valid pair.',
    tradeoff: 'Use a fast, small-memory scan, but the data usually must be ordered.',
  },
  'sliding-window-variable': {
    clue: 'The answer is a contiguous range that can grow and shrink safely.',
    memory: 'Keep two boundaries and counts or totals for the current range.',
    promise: 'After shrinking, the current range satisfies the required rule.',
    step: 'Add the new right item, then move left until the range is valid.',
    why: 'Each possible best range is checked when its right end arrives.',
    repeatedWork: 'Rechecking every range repeats almost all work from nearby ranges.',
    watchOut: 'Remove stale counts when the left boundary moves.',
    tradeoff: 'Reach linear time only when validity changes predictably as boundaries move.',
  },
  'sliding-window-fixed': {
    clue: 'The problem asks about every contiguous block of exactly k items.',
    memory: 'Keep the current k-item range and a value summarizing it.',
    promise: 'Whenever an answer is checked, the summary covers exactly k items.',
    step: 'Add the new item, remove the item leaving the range, then evaluate.',
    why: 'Every length-k range is visited exactly once.',
    repeatedWork: 'Recalculating each range repeats work for its shared items.',
    watchOut: 'Do not evaluate until the first full range has formed.',
    tradeoff: 'Simple totals use little memory; minimums or maximums may need more structure.',
  },
  'prefix-sum': {
    clue: 'A range total can be found from two earlier cumulative totals.',
    memory: 'Keep cumulative values, sometimes with a map of earlier totals.',
    promise: 'Each cumulative value summarizes everything before its position.',
    step: 'Add the next item to the total and compare with an earlier total.',
    why: 'Subtracting the shared beginning leaves only the requested range.',
    repeatedWork: 'Adding every range separately repeats the same early additions.',
    watchOut: 'Start with a zero total so ranges beginning at index zero work.',
    tradeoff: 'Use extra stored totals to answer range questions quickly.',
  },
  'fast-slow': {
    clue: 'Two positions moving at different speeds can reveal distance or a cycle.',
    memory: 'Keep two node references with different speeds or a fixed gap.',
    promise: 'Both references follow the same next links with the intended separation.',
    step: 'Move each reference by its assigned amount and compare their positions.',
    why: 'Their relative movement must meet in a cycle or preserve the needed distance.',
    repeatedWork: 'A visited set can be replaced by comparing moving references.',
    watchOut: 'Check that the faster reference can safely take its next step.',
    tradeoff: 'Use constant memory when every item has one predictable next link.',
  },
  'tree-dfs': {
    clue: 'A node answer can be built from answers returned by its children.',
    memory: 'Keep the current root-to-node path and each child result.',
    promise: 'A finished call returns the correct answer for its entire subtree.',
    step: 'Handle an empty node, solve each child, then combine their results.',
    why: 'Correct child answers can be combined to create the correct parent answer.',
    repeatedWork: 'Recomputing each subtree from its ancestors visits nodes many times.',
    watchOut: 'A one-sided tree can make the active path as long as the tree.',
    tradeoff: 'Recursion mirrors the tree clearly; an explicit stack avoids call limits.',
  },
  'graph-dfs': {
    clue: 'Explore reachability or connected groups one branch at a time.',
    memory: 'Keep visited vertices and a stack of vertices still being explored.',
    promise: 'Every visited vertex was reached by a valid path from the start.',
    step: 'Mark a vertex, then explore each neighbor not visited yet.',
    why: 'Following every edge from reached vertices finds the whole reachable group.',
    repeatedWork: 'Starting fresh searches can revisit the same connected group.',
    watchOut: 'Mark a vertex before expanding it so cycles cannot loop forever.',
    tradeoff: 'Use a narrow stack, but do not expect the fewest-edge path.',
  },
  backtracking: {
    clue: 'Build many possible answers one choice at a time.',
    memory: 'Keep the partial answer and the choices still available.',
    promise: 'The partial answer obeys every rule that can already be checked.',
    step: 'Make one choice, explore it, undo it, then try the next choice.',
    why: 'Every legal sequence of choices appears in the search tree.',
    repeatedWork: 'Continuing after a rule fails explores branches that cannot succeed.',
    watchOut: 'Undo mutable changes before trying another choice.',
    tradeoff: 'Pruning saves work, but the number of possible choices can still grow quickly.',
  },
  bfs: {
    clue: 'Find levels or the fewest equal-cost steps from a starting point.',
    memory: 'Keep a first-in-first-out queue and a visited set.',
    promise: 'Items leave the queue from fewest steps away to most steps away.',
    step: 'Remove the next item, mark unseen neighbors, and add them to the queue.',
    why: 'All shorter paths are explored before any longer path.',
    repeatedWork: 'Exploring whole paths separately repeats their shared beginnings.',
    watchOut: 'Mark neighbors when adding them so they enter the queue only once.',
    tradeoff: 'Guarantee fewest unweighted steps, but the queue can become wide.',
  },
  'topological-sort': {
    clue: 'Prerequisites must be completed before the items that depend on them.',
    memory: 'Keep dependency lists, remaining prerequisite counts, and ready items.',
    promise: 'Every ready item has no unfinished prerequisite.',
    step: 'Take one ready item and reduce the counts of items depending on it.',
    why: 'Only prerequisite-free items are chosen, so the produced order is valid.',
    repeatedWork: 'Repeatedly rescanning every dependency wastes already-known counts.',
    watchOut: 'If not every item is processed, the dependencies contain a cycle.',
    tradeoff: 'Get a valid dependency order by storing the whole dependency graph.',
  },
  dijkstra: {
    clue: 'Find shortest paths when every edge cost is nonnegative.',
    memory: 'Keep best-known distances and a priority queue of promising vertices.',
    promise: 'A vertex removed with its current best distance has the shortest possible distance.',
    step: 'Remove the closest vertex and try improving each neighbor through it.',
    why: 'Nonnegative costs prevent a later path from improving a finished closest vertex.',
    repeatedWork: 'Repeated full scans ignore which unsettled vertex is currently closest.',
    watchOut: 'Skip old queue entries whose distance is no longer current.',
    tradeoff: 'Handle weighted paths efficiently, but not negative edge costs.',
  },
  'limited-shortest-path': {
    clue: 'Find a cheapest path while limiting how many edges it may use.',
    memory: 'Keep the best cost for each vertex at each allowed step count.',
    promise: 'Round i contains the best costs using at most i edges.',
    step: 'Build the next round by trying every edge from the previous round.',
    why: 'Every allowed path ends with one edge added to a shorter allowed path.',
    repeatedWork: 'A normal shortest-path state loses how many steps were used.',
    watchOut: 'Read from the previous round so one round cannot use extra edges.',
    tradeoff: 'Respect the edge limit by storing and processing an extra step dimension.',
  },
  heap: {
    clue: 'Repeatedly choose the smallest or largest currently available item.',
    memory: 'Keep active candidates in a priority queue.',
    promise: 'The queue top is the best active candidate under the chosen order.',
    step: 'Remove the best candidate, use it, and add any candidates it creates.',
    why: 'The priority queue always exposes the next candidate that must be handled.',
    repeatedWork: 'Rescanning all candidates to find the best one repeats linear work.',
    watchOut: 'Remove or ignore entries that are no longer active.',
    tradeoff: 'Get fast best-item access while giving up direct order among other items.',
  },
  'dp-1d': {
    clue: 'The answer for one position depends on answers for earlier positions.',
    memory: 'Keep the solved value for each needed earlier position.',
    promise: 'Before solving position i, every earlier value it uses is correct.',
    step: 'Combine the required earlier answers to compute the current answer once.',
    why: 'Each complete solution ends with one of the choices in the formula.',
    repeatedWork: 'Plain recursion solves the same earlier position many times.',
    watchOut: 'Set base cases before positions that depend on them.',
    tradeoff: 'Store earlier answers to avoid repeated work; sometimes only a few are needed.',
  },
  'dp-grid': {
    clue: 'Each grid cell depends on a small set of nearby solved cells.',
    memory: 'Keep a table or row containing solved cell values.',
    promise: 'Every dependency of the current cell has already been solved.',
    step: 'Combine valid neighboring results to solve the current cell once.',
    why: 'Every valid path reaches a cell from one of the listed neighbors.',
    repeatedWork: 'Exploring paths separately repeats the same remaining grid work.',
    watchOut: 'Initialize borders so missing neighbors are handled consistently.',
    tradeoff: 'Use a table for clarity, or compress rows when only nearby rows matter.',
  },
  'dp-dual': {
    clue: 'The answer depends on two positions, prefixes, or sequences at once.',
    memory: 'Keep a two-dimensional table of already-solved position pairs.',
    promise: 'Each table entry correctly solves its exact pair of prefixes or positions.',
    step: 'Compare the current pair and combine the smaller solved pair states.',
    why: 'Every valid answer must make one of the choices represented by the formula.',
    repeatedWork: 'Recursive branching revisits the same pair of positions.',
    watchOut: 'Define the empty-prefix row and column before filling the rest.',
    tradeoff: 'Avoid repeated branching by using memory proportional to both dimensions.',
  },
  'dp-general': {
    clue: 'Many choices lead back to the same smaller subproblem.',
    memory: 'Keep one answer for every distinct subproblem state.',
    promise: 'A stored state is reused only after its answer is complete.',
    step: 'Solve each possible next choice from smaller states, then keep the required result.',
    why: 'The choices cover every possible final decision without solving a state twice.',
    repeatedWork: 'A direct recursive search solves identical states on different branches.',
    watchOut: 'The state must contain all information that affects future choices.',
    tradeoff: 'Spend memory on states to replace repeated exponential work.',
  },
  memoization: {
    clue: 'A recursive solution reaches the same input state many times.',
    memory: 'Keep a cache from each recursive state to its completed answer.',
    promise: 'Every cached answer is final and correct for exactly that state.',
    step: 'Return a cached answer, or compute it once and save it.',
    why: 'Caching changes only repeated work, not the choices the recursion explores.',
    repeatedWork: 'Different recursive branches recompute identical states.',
    watchOut: 'Include every future-relevant value in the cache key.',
    tradeoff: 'Keep the natural recursive structure while using memory for cached answers.',
  },
  'union-find': {
    clue: 'Edges arrive while the solution tracks which items are connected.',
    memory: 'Keep a parent and small balancing value for each item.',
    promise: 'Items have the same representative exactly when they are connected.',
    step: 'Find both representatives; join them if they are different.',
    why: 'Each join records one new connection between two complete groups.',
    repeatedWork: 'Running a fresh graph search after every edge repeats old connectivity work.',
    watchOut: 'Compress parent paths and join balanced roots to keep operations fast.',
    tradeoff: 'Answer connectivity updates quickly, but do not recover the actual path.',
  },
  trie: {
    clue: 'Many words share beginnings that should be searched together.',
    memory: 'Keep a character tree with links and end-of-word markers.',
    promise: 'The current node represents exactly the characters read so far.',
    step: 'Follow or create the link for the next character.',
    why: 'A path exists exactly when that prefix has been stored.',
    repeatedWork: 'Comparing a query with every word repeats checks of shared prefixes.',
    watchOut: 'A stored prefix is not a complete word without an end marker.',
    tradeoff: 'Make prefix work fast by using more memory than a simple word list.',
  },
  'segment-tree': {
    clue: 'Range questions and value updates must both remain fast.',
    memory: 'Keep a tree whose nodes summarize fixed parts of the array.',
    promise: 'Each node stores the correct combination of its covered range.',
    step: 'Update or query only tree branches whose ranges touch the request.',
    why: 'The selected tree ranges cover the request without gaps or overlap.',
    repeatedWork: 'Recomputing a whole range after each update repeats unaffected work.',
    watchOut: 'Use one consistent rule for range endpoints and child boundaries.',
    tradeoff: 'Use linear memory and more code for fast updates and range queries.',
  },
  intervals: {
    clue: 'The input contains ranges that overlap or must be processed by position.',
    memory: 'Keep intervals sorted and track the current merged or active range.',
    promise: 'All earlier intervals are finalized and cannot overlap future ones unexpectedly.',
    step: 'Compare the next interval with the active one, then merge or finalize.',
    why: 'Sorted starts ensure no later interval can reach behind an already finalized range.',
    repeatedWork: 'Comparing every pair ignores that ordering can make overlap local.',
    watchOut: 'Decide whether touching endpoints count as overlap.',
    tradeoff: 'Pay for sorting so overlap decisions become a single scan.',
  },
  stack: {
    clue: 'The most recently opened or seen unfinished item must be handled first.',
    memory: 'Keep unfinished items in a last-in-first-out stack.',
    promise: 'The stack contains exactly the unfinished items in their required nesting order.',
    step: 'Push new unfinished work; pop when the current item completes it.',
    why: 'Nested work must close in the reverse order that it opened.',
    repeatedWork: 'Searching backward for the latest unfinished item repeats work.',
    watchOut: 'Never pop an empty stack, and check for leftovers at the end.',
    tradeoff: 'Track nested or reverse-order work with memory proportional to unfinished items.',
  },
  'monotonic-stack': {
    clue: 'Each item needs the nearest earlier or later item that is larger or smaller.',
    memory: 'Keep unresolved items in increasing or decreasing stack order.',
    promise: 'Stack items are unresolved and remain in the chosen value order.',
    step: 'Pop items resolved by the new value, then push the new item.',
    why: 'A popped item has found the first value able to resolve it.',
    repeatedWork: 'Scanning outward from every item repeats comparisons.',
    watchOut: 'Choose carefully whether equal values should stay or be popped.',
    tradeoff: 'Get nearest-boundary answers in one pass by storing unresolved items.',
  },
  'monotonic-deque': {
    clue: 'Every moving range needs its minimum or maximum quickly.',
    memory: 'Keep useful range candidates in ordered deque positions.',
    promise: 'The front is the best value in the current range.',
    step: 'Remove expired positions, remove worse back values, then add the new position.',
    why: 'Removed worse values cannot beat the newer value in any future shared range.',
    repeatedWork: 'Scanning each range for its best value repeats comparisons.',
    watchOut: 'Store positions so expired values can be removed.',
    tradeoff: 'Get constant-time range extremes with a more specialized ordered queue.',
  },
  'divide-conquer': {
    clue: 'The input can be split into independent smaller pieces and then combined.',
    memory: 'Keep the active recursive pieces and their returned results.',
    promise: 'Each finished call correctly solves its assigned piece.',
    step: 'Split the input, solve each piece, then combine the answers.',
    why: 'The pieces cover the original input and the combine step restores the whole answer.',
    repeatedWork: 'Working on the full input at every level can repeat smaller work.',
    watchOut: 'Ensure every split gets smaller and base cases stop recursion.',
    tradeoff: 'Gain clean independent subproblems, often with recursion and combine costs.',
  },
  'line-sweep': {
    clue: 'Only the order of starts, ends, or position events matters.',
    memory: 'Keep sorted events and the state currently active at the sweep position.',
    promise: 'Before the next event, active state matches everything covering that position.',
    step: 'Move to the next event and apply all changes at that position.',
    why: 'State changes only at events, so nothing between them needs separate work.',
    repeatedWork: 'Checking every position or pair ignores long stretches with no changes.',
    watchOut: 'Events at the same position need a deliberate processing order.',
    tradeoff: 'Pay for sorting events to process all changes in one ordered pass.',
  },
  greedy: {
    clue: 'A locally best safe choice can never hurt the final answer.',
    memory: 'Keep only the information needed to make the next safe choice.',
    promise: 'The choices already made can still be part of an optimal complete answer.',
    step: 'Choose the best currently safe option and discard choices it makes unnecessary.',
    why: 'Any optimal answer can exchange its next choice for this one without becoming worse.',
    repeatedWork: 'Exploring every sequence of choices is unnecessary once one choice is proven safe.',
    watchOut: 'A choice that looks best is not valid without a proof that it is safe.',
    tradeoff: 'Use little state and one pass when the safe-choice proof truly holds.',
  },
  math: {
    clue: 'A formula or number property can replace direct enumeration.',
    memory: 'Keep only the values required by the formula or number process.',
    promise: 'Each stored value has the exact mathematical meaning used by the formula.',
    step: 'Apply the proven identity or reduce the numbers while preserving the result.',
    why: 'The mathematical identity is equivalent to the condition the problem asks about.',
    repeatedWork: 'Enumerating candidates repeats work captured by a direct formula.',
    watchOut: 'Check integer overflow, signs, rounding, and division rules.',
    tradeoff: 'Gain speed and small memory at the cost of deriving the right identity.',
  },
  matrix: {
    clue: 'Rows, columns, or layers follow a consistent traversal rule.',
    memory: 'Keep boundaries, coordinates, and any visited or result state needed.',
    promise: 'Processed cells are complete and unprocessed cells remain inside the active bounds.',
    step: 'Visit the next cells in the chosen direction, then update the boundary.',
    why: 'The boundary rules cover every required cell exactly as intended.',
    repeatedWork: 'Loose direction changes can revisit cells or require a visited search.',
    watchOut: 'After each boundary change, check whether any row or column remains.',
    tradeoff: 'Boundary traversal can use constant memory but needs careful corner handling.',
  },
  'string-processing': {
    clue: 'Characters can be parsed or transformed in one ordered pass.',
    memory: 'Keep the current token, parsing mode, and output built so far.',
    promise: 'The stored output is correct for every character already read.',
    step: 'Classify the next character and update the current token or output.',
    why: 'Every character is handled once according to the problem rules.',
    repeatedWork: 'Repeated substring creation or rescanning copies the same characters.',
    watchOut: 'Handle the final token and empty input explicitly.',
    tradeoff: 'A one-pass parser is efficient but requires clear state rules.',
  },
  'string-matching': {
    clue: 'Search for a pattern while reusing information from earlier matched characters.',
    memory: 'Keep the current matched length and a table of reusable prefix lengths.',
    promise: 'The matched length is the longest pattern prefix matching the current text ending.',
    step: 'Advance on a match; on a mismatch, fall back to the next reusable prefix.',
    why: 'The fallback table skips only alignments already proven impossible.',
    repeatedWork: 'Restarting from each text position compares the same characters again.',
    watchOut: 'Build and use fallback lengths with consistent indices.',
    tradeoff: 'Guarantee linear matching after extra preprocessing and more complex indexing.',
  },
  'eulerian-circuit': {
    clue: 'Use every directed edge exactly once while building one route.',
    memory: 'Keep ordered outgoing edges and a stack for the route being explored.',
    promise: 'Every removed edge is used once, and finished vertices have no unused outgoing edge.',
    step: 'Follow unused edges until stuck, then add vertices to the answer while returning.',
    why: 'Each edge is consumed once and finished route pieces join into one valid route.',
    repeatedWork: 'Trying complete route orders separately repeats the same edge prefixes.',
    watchOut: 'The completed route is built backward and must be reversed.',
    tradeoff: 'Use every edge efficiently, with ordering work when the smallest route is required.',
  },
  quickselect: {
    clue: 'Only the item at one sorted position is needed, not the whole sorted list.',
    memory: 'Keep the current search boundaries and one pivot position.',
    promise: 'The requested sorted position remains inside the active range.',
    step: 'Partition around a pivot, then continue only on the side containing the target.',
    why: 'Partitioning places the pivot correctly and rules out the opposite side.',
    repeatedWork: 'Fully sorting orders many values whose relative order is never used.',
    watchOut: 'A poor fixed pivot can cause slow worst-case behavior.',
    tradeoff: 'Get linear expected selection while accepting a quadratic worst case.',
  },
  counting: {
    clue: 'Values come from a limited range, so frequencies can replace comparison sorting.',
    memory: 'Keep a count for each possible value or frequency.',
    promise: 'Each count equals how many processed items have that value.',
    step: 'Increase the current value count, then read counts in the needed order.',
    why: 'The counts contain exactly the information needed to rebuild or rank the values.',
    repeatedWork: 'Comparison sorting does more work than needed for a small value range.',
    watchOut: 'Account for the full value range, including offsets or negative values.',
    tradeoff: 'Gain linear counting work when the value range is not too large.',
  },
  'linked-list': {
    clue: 'The answer depends on changing or walking node links in sequence.',
    memory: 'Keep a few node references for the current position and nearby links.',
    promise: 'The finished part has correct links and remains connected to unfinished work.',
    step: 'Save any link still needed, change the current links, then advance.',
    why: 'Saving the next link before changing pointers prevents losing the remaining list.',
    repeatedWork: 'Repeatedly searching from the head turns one pass into many passes.',
    watchOut: 'Handle an empty list, one node, and changes to the head.',
    tradeoff: 'Change links with constant memory, but pointer update order must be exact.',
  },
}
